const Scheduler = require('../src/services/scheduler');
jest.mock('../src/services/jsonStorage');

describe('Scheduler', () => {
  test('should check notifications every minute', () => {
    // Тест планировщика
  });
});